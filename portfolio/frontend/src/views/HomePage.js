import React from 'react';

const HomePage = () => {
  return (
    <div styled={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '90vh'
    }}>
      Home
    </div>
  );
};

export default HomePage;
